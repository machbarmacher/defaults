name: MachbarMacher project defaults dependencies
type: module
description: 'shield, backup_migrate, composer_deploy, drush language (via composer).'
package: MachbarMacher
core: 8.x
dependencies:
  - shield
  - backup_migrate
  - composer_deploy

