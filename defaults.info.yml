name: MachbarMacher project defaults
type: module
description: 'Shield, backup, drush language.'
package: MachbarMacher
core: 8.x
config: entity.address_format.collection
dependencies:
  - shield
  - backup_migrate
  - drush_language
  - composer_deploy

